# DC-DC 硬件问题答案

## 1. DC-DC 和 LDO 有什么区别？分别适合什么场景？

DC-DC 是开关电源，靠电感/电容储能和开关管调节电压，效率高，能升压、降压或反相，但纹波和 EMI 较大。

LDO 是线性稳压器，结构简单、噪声低、响应快、外围少，但只能降压，效率约为：

```text
效率 ~= Vout / Vin
```

适用场景：

- DC-DC：大电流、大压差、效率敏感、系统主电源。
- LDO：小电流、低噪声、模拟/RF/ADC/DAC 电源，或 DC-DC 后级二次净化。

## 2. Buck、Boost、Buck-Boost 三种拓扑能不能画出来？输出电压公式会不会推？

### Buck 降压

```text
Vin -- SW -- L -- Vout
              |
             Cout
              |
             GND
```

理想连续导通模式 CCM：

```text
Vout = D * Vin
```

### Boost 升压

```text
Vin -- L -- SW node -- D -- Vout
             |             |
            MOS           Cout
             |             |
            GND           GND
```

理想 CCM：

```text
Vout = Vin / (1 - D)
```

### 反相 Buck-Boost

```text
Vin -- SW -- L -- GND
              |
              D
              |
            Vout(-)
```

理想 CCM：

```text
Vout = -D / (1 - D) * Vin
```

非反相 Buck-Boost 通常由 Buck + Boost 组合实现，公式按工作区间分别接近 Buck 或 Boost。

## 3. 什么是伏秒平衡？为什么电感设计一定绕不开它？

伏秒平衡指稳态下电感一个周期内平均电压为 0：

```text
integral(VL dt) = 0
```

否则电感电流会持续上升或下降，最终饱和或断流。

所有 DC-DC 输出电压公式，本质都是从电感伏秒平衡推出来的，所以电感设计绕不开它。

## 4. DC-DC 电感怎么选？电感值、饱和电流、DCR 哪个最容易被忽略？

主要看：

- 电感值 L：决定电感纹波电流。L 太小，纹波大、峰值电流高；L 太大，响应慢、体积大。
- 饱和电流 Isat：必须大于最大峰值电感电流，否则电感饱和，电流暴涨。
- 温升电流 Irms：看长期发热能力。
- DCR：直流电阻，越大铜损越大，效率越低。
- 磁芯损耗：高频下不能只看 DCR。

最容易被忽略的是饱和电流和 DCR。很多人只按电感值选型，结果满载、瞬态或高温时电感饱和，或者效率异常低。

## 5. 输出纹波应该怎么测？为什么很多人一测就测错？

正确测法：

- 示波器探头用短地弹簧，不要用长地线。
- 探头直接贴近输出电容两端测。
- 开启带宽限制，例如 20 MHz，按规格要求来。
- 区分纹波、开关尖峰、噪声。
- 使用 AC 耦合观察小纹波，但注意不要误判直流电平。

常见错误是用长鳄鱼夹地线，形成大环路，把空间 EMI 当成输出纹波，测出来的尖峰会严重偏大。

## 6. DC-DC 纹波过大应该先查哪些地方？

优先检查：

- 输出电容容量、ESR、ESL 是否合适。
- 电感值是否过小，电感纹波电流是否过大。
- 布局是否让开关电流环路过大。
- 输入电容是否靠近芯片 VIN/GND。
- 反馈线是否靠近 SW 节点或大电流路径。
- 负载瞬态是否超出环路能力。
- 是否实际测量方法错误。

先排测量方法，再排布局和输出 LC，效率最高。

## 7. 效率受哪些因素影响？MOS、电感、频率、布局分别怎么影响损耗？

主要损耗：

- MOS 导通损耗：约为 I^2 * Rds_on。
- MOS 开关损耗：频率越高、Vin 越高、开关沿越慢，损耗越大。
- 二极管损耗：非同步整流中约为 Vf * I，大电流时很明显。
- 电感 DCR 损耗：约为 Irms^2 * DCR。
- 磁芯损耗：随频率、磁通摆幅、材料变化。
- 开关频率：频率高，体积可小，但开关损耗和磁芯损耗增加。
- 布局：寄生电感/电阻增大，会增加尖峰、振铃、EMI 和额外损耗。

## 8. 什么是瞬态响应？负载突然变化时输出为什么会跌落或者过冲？

瞬态响应是负载电流突然变化时，电源维持输出稳定的能力。

负载突然增大时，电感电流不能瞬间增加，输出电容先放电，所以 Vout 会跌落。

负载突然减小时，电感电流不能瞬间减小，多余能量灌入输出电容，所以 Vout 会过冲。

影响因素包括环路带宽、补偿设计、输出电容、电感值、限流策略和负载阶跃速度。

## 9. 电感为什么会“吱吱”叫？是电感坏了还是控制环路、负载材料的问题？

常见原因：

- 磁致伸缩：磁芯材料在交变磁场下机械振动。
- 绕组受力振动。
- 轻载 PFM/跳脉冲模式频率落入音频范围。
- 环路不稳定导致低频振荡。
- 陶瓷电容压电效应，有时声音不一定来自电感。

不一定是电感坏了。若只在轻载叫，常见原因是省电模式；若满载也叫或伴随输出振荡，要重点查补偿、布局、电感饱和和负载特性。

## 10. DC-DC 的 EMI 严重，应该从开关回路、布局、滤波还是频率扩展入手？

优先顺序：

- 先看开关热回路：输入电容、MOS、高边/低边开关路径要最小化。
- 再看布局：SW 节点面积尽量小，远离反馈和敏感信号。
- 检查输入滤波：必要时加 pi 滤波、磁珠、共模电感，但不能破坏稳定性。
- 检查吸收/阻尼：SW 振铃严重时加 RC snubber。
- 再考虑频率扩展 Spread Spectrum：它能摊平 EMI 峰值，但不能解决烂布局。
- 最后优化屏蔽、接地、走线层叠和器件选型。

核心原则：EMI 首先是高 di/dt 回路和高 dv/dt 节点的问题，不能一上来只靠滤波器硬压。
